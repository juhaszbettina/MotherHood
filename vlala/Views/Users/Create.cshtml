@model MotherHood.Models.Message

@{
    ViewData["Title"] = "Create";
}

<h1>Létrehozás</h1>

<h4>Üzenet</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            @*<div class="form-group">
                <select asp-for="Tema" asp-items="Html.GetEnumSelectList<Tema>()">
                    <option selected="selected" value="">Válassz témát...</option>
                </select>
                <span asp-validation-for="Tema" class="text-danger" />
            </div>*@

            @Html.DropDownList("Tema", Html.GetEnumSelectList<Tema>(), "Válassz témát!", new { @class = "form-control" }) <br >
            <div class="form-group">
                <label asp-for="Cim" class="control-label"></label>
                <input asp-for="Cim" class="form-control" />
                <span asp-validation-for="Cim" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Uzenet" class="control-label"></label>
                <textarea asp-for="Uzenet" class="form-control">  </textarea>
                <span asp-validation-for="Uzenet" class="text-danger"></span>
            </div>

            <div class="form-group">
                <input type="submit" value="Küldés" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Vissza a listára</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
